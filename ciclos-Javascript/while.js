// Para programar un ciclo necesitamos de una variable llamada contador 

let contador = 0;
while(contador < 3){
    console.log(contador);
    contador++;
}

console.log("Fin del ciclo while ")

// El anterior ejercicio es un ejemplo de un ciclo, primero declaramos una variable contador y luego se impone una condición 
// La condición será que si contador es menor que tres, entonces se muestra el valor de contador, se incrementa su valor y se vuelve a ejecutar el ciclo while 